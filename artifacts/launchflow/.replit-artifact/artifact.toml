kind = "web"
previewPath = "/"
title = "LaunchFlow AI"
version = "1.0.0"
id = "artifacts/launchflow"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20143

[services.development]
run = "pnpm --filter @workspace/launchflow run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/launchflow", "run", "build" ]
publicDir = "artifacts/launchflow/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20143"
BASE_PATH = "/"
